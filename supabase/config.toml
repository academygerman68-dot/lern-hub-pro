# Supabase project link (optional local CLI)
project_id = "omxemusaqgzkogqvcdfw"

[api]
enabled = true

[db]
major_version = 17

[auth]
enabled = true

[storage]
enabled = true

# JWT is still validated inside the function (Bearer + auth.getUser).
# verify_jwt remains true so the gateway rejects missing/invalid tokens early.
[functions.jaas-token]
verify_jwt = true

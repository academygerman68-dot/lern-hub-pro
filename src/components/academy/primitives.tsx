/**
 * Academy UI primitives — single entry for shared presentation components.
 * Implementation lives in ./shared/design-system to keep this import path stable.
 */
export {
  AvatarName,
  EmptyState,
  FileDropzoneVisual,
  FilterBar,
  FormSection,
  GroupBadge,
  LevelBadge,
  Metric,
  PageHeader,
  PageShell,
  PremiumHeader,
  ProgressLine,
  SectionHeader,
  SectionTitle,
  StatCard,
  Status,
  StatusBadge,
  Surface,
  TableScroll,
  Toolbar,
} from "./shared/design-system";
